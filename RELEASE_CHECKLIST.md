# Релизный чеклист

Дата проверки: 2026-06-14.

## Статус

- [x] Release candidate.
- [x] Готово к закрытому экспертному показу.
- [x] Runtime не зависит от внешних источников.

## Самодостаточность

- [x] Внешних runtime-зависимостей нет.
- [x] Plotly хранится локально: `docs/assets/vendor/plotly/plotly.min.js`.
- [x] Three.js хранится локально: `docs/assets/vendor/three/three.module.js`.
- [x] OrbitControls хранится локально: `docs/assets/vendor/three/OrbitControls.js`.
- [x] Авторский прогноз СКР и численности хранится локально.
- [x] Прогноз городского и сельского СКР для `Расселения` рассчитан локально и хранится в `docs/data/settlement_tfr_forecast_2050.json`.
- [x] Исходные файлы авторского прогноза сохранены в `docs/data/author_forecast_source/`.
- [x] Горизонт runtime-данных ограничен 2050 годом.
- [x] Favicon задан локальными icon-only файлами `docs/assets/img/favicon.png` и `docs/favicon.ico`, без текста логотипа.
- [x] Runtime-файл Маткапитала хранится локально: `docs/data/matcapital_inputs.json`, `runtime_external_fetch:false`, внешних URL внутри файла нет.
- [x] Маткапитал использует ключ `comfortable_housing_model` и видимую формулировку `подход по стоимости комфортного жилья`.

## Страницы

- [x] `СКР` — `docs/index.html`.
- [x] `Расселение` — `docs/settlement.html`.
- [x] `Усадьба` — `docs/estate.html`.
- [x] `Маткапитал` — `docs/capital.html`.
- [x] `Ипотека` — `docs/mortgage.html`.
- [x] `Выплаты` — `docs/payments.html`.

## Проверки

- [x] `python scripts/check_matcapital_module.py`.
- [x] `python scripts/check_settlement_forecast.py`.
- [x] `python scripts/check_json.py`.
- [x] `python scripts/check_no_external_runtime.py`.
- [x] `python scripts/check_russian_ui.py`.
- [x] `python scripts/check_data_locality.py`.
- [x] `python scripts/check_nav_numbering.py`.
- [x] `"C:\Program Files\Git\bin\bash.exe" scripts/check_js_syntax.sh`.
- [x] `python scripts/audit_all.py`.
- [x] `npm install`.
- [x] `npm run test:smoke` — 30/30, включая smoke и Playwright visual QA.

## Browser QA

- [x] Страницы открыты локально с сервера `python -m http.server 8000 --directory docs`.
- [x] Проверены desktop и mobile состояния.
- [x] Проверены основные интеракции: режимы СКР, краткий/подробный вид, выбор территории, сценарии расселения, этажность, участок в сотках, отдельный дом для прародителей, 3D-размерный слой усадьбы, Маткапитал, ипотека, выплаты, выгрузки таблиц.
- [x] Страница `Расселение` использует `settlement_tfr_forecast_2050.json`: `SettlementModule.getState().forecastMethod === "local_gp_ucm_ensemble"`, при сценарии `фиксация` сценарный СКР совпадает с базовой траекторией, отрицательных прогнозных разрывов город/село нет, а рост доли среды не снижает сценарный СКР.
- [x] Playwright visual QA проверяет все видимые Plotly-графики, отсутствие грубых наложений подписей, геометрию картограммы, синхронизацию запуска мер с лагом 9 месяцев и сброс камеры 3D-сцены.
- [x] 3D-блок усадьбы проверен на широком viewport, после resize, после CSS zoom 92%, после мобильного resize и после сброса вида.
- [x] Камера 3D-усадьбы по умолчанию и после изменения настроек показывает лицевую сторону дома: видны вход, крыльцо и дорожка.
- [x] Этажность 1–4 меняет высоту дома и уменьшает пятно застройки без изменения жилой площади.
- [x] Забор строится с разрывом под вход/въезд; дополнительная калитка появляется только при включённом отдельном доме для прародителей.
- [x] Слой размерных линий в метрах скрыт по умолчанию и включается переключателем `показать размеры`.
- [x] Размеры усадьбы переведены в проектные 3D-линии на земле с тонкими чёрными стрелками, end-cap ограничителями, крупными прозрачными подписями и отдельной линией высоты дома; включение и выключение размеров не меняет камеру, target и distance.
- [x] Дверь и окна получили реалистичный масштаб: входная дверь 1,05 × 2,15 м, фасадное окно 1,35 × 1,40 м; задние окна развёрнуты наружу и проверяются Playwright-контрактом `windowOrientationDiagnostics`.
- [x] Pixel-QA проверяет реальные пиксели canvas, крупное заполнение 3D-блока и уход модели в правый нижний угол.
- [x] 3D-камера усадьбы настроена по bounds основного дома: дом не обрезается, участок может слегка доходить до края ради более крупного вида.
- [x] Крыша 3D-дома проверена на стабильный объёмный непрозрачный рендер: фасетные скаты, конёк, свесы, корректная ориентация граней, отключён приём теней крышей, добавлен pixel-QA на стабильность двух последовательных кадров.
- [x] Управление составом семьи уточнено: `Взрослые в ядре семьи` заменено на `Родители`, максимум родителей ограничен 2; `Родители / прародители рядом` заменено на `Прародители рядом`.
- [x] Верхний блок `Состав семьи и нормативы` переработан: 2×2 stepper-карточки с большими цифрами, стрелками, без маленького дубля числа под карточкой, и отдельным видимым выбором участка в сотках.
- [x] Preset-кнопки `старт`, `многодетная`, `3 поколения` удалены; пользователь настраивает состав семьи, этажность, участок и отдельный дом для прародителей вручную.
- [x] Функция удалённой работы / семейного микробизнеса удалена из UI, расчётов, легенды, таблицы и выгрузки.
- [x] Контроль `Участок (соток)` и чекбокс `Отдельный дом для прародителей` проверены; чекбокс по умолчанию выключен.
- [x] Деревья размещаются вне защитных зон основного дома, отдельного домика, дорожек и сада; Playwright проверяет `treeCollisionCount === 0` для 4, 12 и 30 соток.
- [x] In-app Browser проверил `settlement.html`: page identity, nonblank DOM, видимый Plotly-график `СКР по типам местности и сценарный общий СКР`, 7 трасс, отсутствие внешних ассетов и console errors/warnings; при изменении доли среды KPI эффекта вырос с `+0,002` до `+0,005`.
- [x] In-app Browser проверил `estate.html`: page identity, nonblank DOM snapshot, canvas present, reset visible, отсутствие preset-кнопок и микробизнес-модуля, отдельный дом для прародителей on/off, 3D-размеры и линия высоты, desktop/mobile screenshots, console errors/warnings отсутствуют.
- [x] In-app Browser проверил `capital.html`: page identity, navigation, 4 Plotly-графика, KPI, таблица, изменение параметров, итоговые плашки `Итого за 4 детей`, выгрузка таблицы, desktop/mobile screenshots, console errors/warnings отсутствуют.
- [x] Видимые названия страниц в навигации и надзаголовках показаны без отвлекающих числовых префиксов.
- [x] Контрольные скриншоты сохранены в `artifacts/visual-qa/`.
- [x] Внешних сетевых запросов при Browser/Playwright QA не обнаружено.
- [x] Релизный архив собран: `C:\Codex projects\tsargrad_demographic_platform_selfcontained_release_github_pages.zip`.
- [x] Архив проверен: содержит `docs/capital.html`, `docs/assets/js/capital.js`, `docs/data/matcapital_inputs.json`, не содержит `node_modules`, `artifacts`, `.playwright-mcp`, `test-results`.

## Ограничения

Невыполненных обязательных проверок нет.
